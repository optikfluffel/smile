%h3 Users
%ul.users
  - @users.each do |user|
    %li
      = link_to user.username, user
      signed up #{user.created_at.to_date}
      = link_to "Follow", followings_path(:follower_id => user), :method => :post

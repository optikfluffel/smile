%h3 Users
- if @users.length >= 1
  %ul.users.follow.unstyled
    - @users.each do |user|
      %li
        = gravatar_image_tag(user.email, :gravatar => { :secure => true, :size => 23 })
        = link_to user.username, user
        = link_to "Follow", followings_path(:follower_id => user), :method => :post, :class => "button tiny radius"
- else
  %p There is no user here you don't follow yet. Invite your friends to get some more ;)